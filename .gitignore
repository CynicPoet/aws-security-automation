# Terraform
**/.terraform/
*.tfstate
*.tfstate.backup
*.tfstate.lock.info
*.tfvars
!*.tfvars.example
.terraform.lock.hcl
terraform.plan

# Python
__pycache__/
*.py[cod]
*.pyo
.pytest_cache/
*.egg-info/
dist/
build/
venv/
.env

# Lambda deployment packages
*.zip

# AWS credentials — never commit these
.aws/
credentials

# OS
.DS_Store
Thumbs.db

# VS Code
.vscode/
*.code-workspace

# Secrets
*.pem
*.key
secrets.json

# Local deploy config (contains credentials — never commit)
scripts/config.ps1

# Internal notes — not for public repo
KNOWLEDGEBASE.md
docs/judge-qa.md

# Architecture diagram folder (local tool, generated output)
diagram/
